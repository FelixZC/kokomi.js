{
  "compilerOptions": { // 编译器选项配置
    /* Basic Options */
    "moduleResolution": "node",
    "baseUrl": "./src",
    "paths": {
      "*": ["node_modules/*"]
    },
    "target": "ESNext", // 指定 ECMAScript 目标版本
    "module": "ESNext", // 指定模块代码生成方式
    "lib": [ // 指定要包含在编译中的库文件
      "DOM",
      "ESNext",
      "DOM.Iterable"
    ],
    "declaration": true, // 生成对应的 '.d.ts' 文件
    "outDir": "./dist", // 将输出结构重定向到目录
    "rootDir": "./src", // 指定输入文件的根目录，用于控制 --outDir 下的输出目录结构

    // ... 更多选项 ...

    "skipLibCheck": true, // 跳过声明文件的类型检查
    "forceConsistentCasingInFileNames": true // 禁止对同一文件的不一致大小写引用
  }
}